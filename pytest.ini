[tool.pytest.ini_options]
# E2E test configuration
addopts = "-v"
testpaths = ["tests"]

# Playwright browser arguments
# These prevent popups and restore dialogs
[tool.pytest.playwright]
browser_channel = "chromium"
